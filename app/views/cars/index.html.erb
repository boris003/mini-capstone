<h1> List of cars</h1>


<% @cars.each do |car| %>
  <h2><%= car.make %> <%= car.model %> <%= car.year %> <%= car.engine %> <%= car.color %> $<%= car.price %></h2>
  <%= button_to "Edit", {action: "edit", id: car.id}, method: :get %>
  <%= button_to "Delete", {action: "destroy", id: car.id}, method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>